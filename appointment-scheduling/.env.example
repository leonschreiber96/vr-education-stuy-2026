# ===========================================
# TERMINFINDUNG - Production Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=3000

# Base URL of your application (no trailing slash)
# Examples: https://terminfindung.example.com or http://your-vps-ip:3000
BASE_URL=https://terminfindung.example.com

# Base path if serving under a subpath (e.g., /masterarbeit)
# Leave empty if serving at root
# Examples: /masterarbeit or /app or empty for root
BASE_PATH=

# ===========================================
# SECURITY CONFIGURATION
# ===========================================

# Session secret - MUST be changed in production!
# Generate a random string with: openssl rand -base64 32
SESSION_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING_IN_PRODUCTION

# Admin password for the default admin account
# This will be used to create/reset the admin account
ADMIN_PASSWORD=your_secure_admin_password_here

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DB_PATH=/app/data/appointments.db

# ===========================================
# STUDY CONFIGURATION
# ===========================================

# Target number of participants for the study
PARTICIPANT_GOAL=80

# Follow-up appointment scheduling rules (in days after primary)
# FOLLOWUP_MIN_DAYS=29
# FOLLOWUP_MAX_DAYS=31

# ===========================================
# EMAIL CONFIGURATION
# ===========================================

# Enable/disable email functionality
MAIL_ENABLED=true

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Authentication
# For Gmail: Use an "App Password" (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Email Addresses
ADMIN_EMAIL=admin@example.com
MAIL_FROM=noreply@example.com

# ===========================================
# NGINX / SSL CONFIGURATION (for docker-compose)
# ===========================================

# Your domain name for SSL certificate
DOMAIN=terminfindung.example.com

# Email for Let's Encrypt notifications
LETSENCRYPT_EMAIL=admin@example.com

# ===========================================
# NOTES
# ===========================================
#
# Required Variables (must be changed):
# - SESSION_SECRET
# - ADMIN_PASSWORD
# - BASE_URL
#
# Email Configuration:
# - If MAIL_ENABLED=false or SMTP credentials are missing,
#   emails will be logged to console only
# - For Gmail, enable 2FA and create an App Password
# - For other providers, adjust SMTP_HOST, SMTP_PORT, and SMTP_SECURE
#
# SSL/HTTPS:
# - The included nginx configuration supports automatic SSL via Let's Encrypt
# - Make sure your domain points to your VPS IP before running certbot
# - Initial setup uses HTTP, then run the SSL setup script
#
# Security Best Practices:
# - Generate SESSION_SECRET with: openssl rand -base64 32
# - Use a strong ADMIN_PASSWORD (12+ characters, mixed case, numbers, symbols)
# - Never commit .env to git
# - Restrict .env file permissions: chmod 600 .env
#
